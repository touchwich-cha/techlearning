# Quote form delivery endpoint (an n8n webhook URL or a Google Apps Script /exec URL).
# Leave empty to fall back to opening the visitor's email client (mailto) with the form data.
# NOTE: NEXT_PUBLIC_* values are inlined at build time — redeploy after changing this.
NEXT_PUBLIC_QUOTE_ENDPOINT=
